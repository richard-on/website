apiVersion: v1
kind: Service
metadata:
  labels:
    app: website
  name: website
spec:
  internalTrafficPolicy: Local
  ports:
    - name: "http"
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: website
